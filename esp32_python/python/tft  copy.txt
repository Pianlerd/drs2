#include <TFT_eSPI.h>  // ไลบรารี TFT_eSPI
#include <TFT_eSPI.h>  // ไลบรารี TFT_eSPI
#include <SPI.h>

TFT_eSPI tft = TFT_eSPI();  // สร้างอ็อบเจ็กต์ TFT

void setup() {
  tft.init();              // เริ่มต้นจอ
  tft.setRotation(1);      // หมุนจอ landscape
  tft.fillScreen(TFT_BLACK); // ล้างจอสีดำ

  // วาดตัวอย่าง
  tft.setTextColor(TFT_WHITE, TFT_BLACK);
  tft.setTextSize(2);
  tft.drawString("Hello ESP32 TFT!", 50, 150);
/*
  tft.drawRect(20, 50, 200, 100, TFT_RED);   // วาดกรอบสีแดง
  tft.fillRect(250, 50, 100, 50, TFT_BLUE);  // วาดสี่เหลี่ยมสีน้ำเงิน*/
}

void loop() {
  // ตัวอย่างแอนิเมชัน
 /*tft.fillCircle(random(480), random(320), 10, TFT_GREEN);
  delay(100);*/
}